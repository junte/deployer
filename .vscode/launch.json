{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "app",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/src/cmd/server",
      "args": [
        "--config=tools/dev/config.yaml"
      ],
      "cwd": "${workspaceFolder}",
    }
  ]
}