{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "app",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/server",
            "args": [
                "--config=dev/config.yaml"
            ],
            "cwd": "${workspaceFolder}",
        }
    ]
}